# Translation Server Configuration
# Copy this file to .env and fill in your values
#
# cp .env.example .env

# ============================================================================
# OpenAI Realtime API (Recommended)
# ============================================================================
# Get your API key at: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# Voice: alloy, echo, fable, onyx, nova, shimmer
OPENAI_VOICE=alloy

# ============================================================================
# Vertex AI Configuration (Gemini Live API)
# ============================================================================
# Requires: Service Account with roles/aiplatform.user
# Set GOOGLE_APPLICATION_CREDENTIALS to path of credentials.json

# Google Cloud Project ID (required)
GOOGLE_CLOUD_PROJECT=your-project-id

# Vertex AI region - europe-west4 recommended for EU (low latency for Italy)
# Available: us-central1, europe-west4, asia-northeast1, etc.
GOOGLE_CLOUD_LOCATION=europe-west4

# ============================================================================
# Server Configuration (Optional)
# ============================================================================
SERVER_PORT=8001
SERVER_HOST=0.0.0.0
LOG_LEVEL=info

# ============================================================================
# Vertex AI Gemini Model Configuration (Optional)
# ============================================================================
# Available models for Vertex AI Live API:
# - gemini-2.5-flash-live-preview-04-09 (recommended, stable preview)
# - gemini-live-2.5-flash (latest)
# - gemini-live-2.5-flash-native-audio (native audio support)
GEMINI_MODEL=gemini-2.5-flash-live-preview-04-09

# Voice for synthesized speech output
# Available voices: Aoede, Charon, Fenrir, Kore, Puck
GEMINI_VOICE=Kore

# ============================================================================
# Translation Mode (OpenAI)
# ============================================================================
# Available modes:
# - vad: Server VAD - translation starts when silence is detected (default)
# - periodic: Periodic - translation every N seconds while speaking
TRANSLATION_MODE=vad

# ============================================================================
# VAD Mode Settings (used when TRANSLATION_MODE=vad)
# ============================================================================
# These settings control automatic speech detection

# VAD sensitivity threshold (0.0-1.0, higher = less sensitive)
VAD_THRESHOLD=0.5

# Audio to include before speech detection (ms)
VAD_PREFIX_PADDING_MS=300

# Silence duration to trigger translation (ms)
# Lower = faster response, Higher = better for long pauses
VAD_SILENCE_DURATION_MS=500

# ============================================================================
# Periodic Mode Settings (used when TRANSLATION_MODE=periodic)
# ============================================================================
# Interval between translations (ms)
# Lower = more frequent updates, Higher = better context
PERIODIC_INTERVAL_MS=3000

# ============================================================================
# Translation Defaults (Optional)
# ============================================================================
# Source language: auto, de, es, en, fr, it
DEFAULT_SOURCE_LANG=auto

# Target language: de, es, en, fr, it
DEFAULT_TARGET_LANG=it
